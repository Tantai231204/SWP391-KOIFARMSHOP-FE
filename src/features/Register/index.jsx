
import RegisterForm from './RegisterForm';

Register.propTypes = {
    
};

function Register() {
    return (
        <div className='register-page-container'>
         <RegisterForm/>
        </div>
    );
}

export default Register;