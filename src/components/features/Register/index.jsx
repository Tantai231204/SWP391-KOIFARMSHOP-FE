
import PropTypes from 'prop-types';

Register.propTypes = {
    Register: PropTypes.func,
};

function Register() {
    return (
        <div>
            Register
        </div>
    );
}

export default Register;